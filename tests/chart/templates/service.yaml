apiVersion: v1
kind: Service
metadata:
  name: example-api
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: example-api
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
